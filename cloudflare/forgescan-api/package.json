{
  "name": "forgescan-api",
  "version": "0.1.0",
  "description": "ForgeScan 360 API - Cloudflare Worker",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --platform=browser --target=es2022 --main-fields=module,main",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:prod": "npm run build && wrangler deploy --env production",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "pdf-lib": "^1.17.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
