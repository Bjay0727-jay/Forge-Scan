# ForgeScan Scanner — Environment Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Get these values from the ForgeScan dashboard:
#   Settings → Scanners → Register New Scanner

# Required: Scanner API key (from dashboard registration)
FORGESCAN_SCANNER_API_KEY=

# Required: Scanner ID (from dashboard registration)
FORGESCAN_SCANNER_ID=

# Optional: Platform API URL (default: production)
FORGESCAN_PLATFORM_URL=https://forgescan-api.stanley-riley.workers.dev

# Optional: Log level (trace, debug, info, warn, error)
FORGESCAN_LOG_LEVEL=info

# ── Xiid SealedTunnel Mode ────────────────────────────────────
# Uncomment the line below to route scanner traffic through
# Xiid SealedTunnel instead of direct HTTPS to Cloudflare.
# Requires STLink to be running on this host (see deploy/xiid/).
#
# FORGESCAN_PLATFORM_URL=http://127.0.0.5:443
